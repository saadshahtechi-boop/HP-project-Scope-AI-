{
  "name": "techciko-health-suite",
  "version": "1.0.0",
  "description": "Techciko Health Suite — AI Smart Clinic Management System (Next.js + NestJS + Prisma + PostgreSQL)",
  "private": true,
  "workspaces": ["apps/web", "apps/api"],
  "scripts": {
    "dev:web": "npm --prefix apps/web run dev",
    "dev:api": "npm --prefix apps/api run start:dev",
    "build:web": "npm --prefix apps/web run build",
    "build:api": "npm --prefix apps/api run build",
    "install:all": "npm install --prefix apps/web && npm install --prefix apps/api",
    "docker:up": "docker compose up --build"
  },
  "keywords": ["healthcare", "clinic-management", "nextjs", "nestjs", "prisma", "postgresql", "typescript"],
  "license": "MIT"
}
